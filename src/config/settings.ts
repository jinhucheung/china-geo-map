const settings : {
  site_title: string,
  site_toolbar: {
    zoom_out: string,
    zoom_in: string,
    default: string
  }
} = {
  site_title: '中国地图',
  site_toolbar: {
    zoom_out: '缩小',
    zoom_in: '放大',
    default: '默认地图'
  }
}

export default settings
